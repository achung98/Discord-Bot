import net.dv8tion.jda.core.entities.Member;

public class Warning {
	private UsersLinkedList userList = new UsersLinkedList();
	
	public boolean checkMember(Member member) {
		if(userList.contains(member)) {
			if(userList.getWarning(member) != 3) {
				int warnings = userList.getWarning(member)+1;
				userList.setWarning(member, warnings);
			} else {
				return true;
			}
		}
		userList.add(member);
		return false;
	}
}
